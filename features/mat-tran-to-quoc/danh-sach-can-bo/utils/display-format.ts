export {
  trimmedDisplay,
  phoneDigits as canBoPhoneDigits,
  formatPhoneDisplay as formatCanBoPhoneDisplay,
  phoneTelHref as canBoPhoneTelHref,
  formatListDate as formatCanBoListDate,
  formatDetailDate as formatCanBoDetailDate,
  formatListDateTime as formatCanBoListDateTime,
} from '@/lib/display-format';
