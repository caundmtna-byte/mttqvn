# Dependencies & build
node_modules/
.npm-cache/
dist/
coverage/

# Vite / Vitest cache
node_modules/.vite/

# Environment (secrets)
.env
.env.*
!.env.example
.env.local
.env*.local

# Bản sao lưu database — CHỨA DỮ LIỆU THẬT của cán bộ.
# Chỗ đúng là NGOÀI repo (~/Desktop/mttqvn-db-backups, xem scripts/backup-db.sh).
# Các mẫu dưới đây chỉ là lưới an toàn phòng khi ai đó lỡ dump vào thư mục dự án.
mttqvn-db-backups/
/mttqvn-*.sql
*.sql.dang-ghi
*.dump

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Local Word templates at repo root (use docs/templates/ instead)
/*.docx

# IDE (optional)
.idea/
*.swp
*.swo

# TypeScript incremental build cache
*.tsbuildinfo

# Supabase CLI cache — project ref, pooler URL, phiên bản dịch vụ.
# Không phải bí mật, nhưng đổi theo từng máy và lộ topo hạ tầng, không nên
# nằm trong lịch sử git.
supabase/.temp/
